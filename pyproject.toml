[project]
name = "location"
version = "0.1.0"
description = "File-based location"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "black>=26.5.1",
    "pylint>=4.0.6",
    "ty>=0.0.55",
]
